# ML Study Buddy

An LLM-powered agent that quizzes you on machine learning topics and
tracks where you're weak. Built incrementally as a learning project.

## Setup
1. `pip install -r requirements.txt`
2. Copy `.env.example` to `.env` and add your OpenAI key.

## Usage
(coming soon)